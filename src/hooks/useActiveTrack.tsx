import { Track } from "@/components/TrackListItem";
import { useEffect, useState } from "react";

export default function useActiveTrack() {
  const [activeTrack, setActiveTrack] = useState<Track>()
 
    useEffect(() => { 
        
    })
}
